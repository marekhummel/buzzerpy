[project]
name = "buzzer-py"
version = "2.0.0"
description = "A simple real-time quiz buzzer built with Flask and Socket.IO."
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "eventlet",
    "flask",
    "flask-socketio",
]

[dependency-groups]
dev = [
    "mypy",
    "ruff",
    "types-flask-socketio",
]

[tool.poetry]
package-mode = false

[tool.mypy]
files = ["buzzapp"]
check_untyped_defs = true
